import { gsap } from 'gsap'; import { ScrollTrigger } from 'gsap/ScrollTrigger'; import { SplitText } from 'gsap/SplitText';
export function initHero(reduced) { const hero = document.querySelector('.hero'); if (reduced) return; const date = new SplitText('.hero__date', { type: 'chars' }); const tl = gsap.timeline(); tl.from('.site-header', { y: -28, autoAlpha: 0, duration: .8, ease: 'power3.out' }).from('.hero__meta, .scroll-cue', { autoAlpha: 0, y: 18, stagger: .12, duration: .75 }, '-=.45').from(date.chars, { autoAlpha: 0, y: 12, stagger: .04, duration: .5 }, '-=.55').from('.hero__title span', { yPercent: 112, stagger: .12, duration: 1.25, ease: 'power4.out' }, '-=.75').from('.hero__portrait', { scale: 1.18, clipPath: 'inset(0 0 100% 0)', duration: 1.4, ease: 'power4.out' }, '-=1.25'); gsap.timeline({ scrollTrigger: { trigger: hero, start: 'top top', end: 'bottom top', scrub: 1 } }).to('.hero__title span:first-child', { xPercent: -22 }).to('.hero__title span:last-child', { xPercent: 20 }, 0).to('.hero__portrait', { scale: 1.08 }, 0).to('.hero__date, .hero__meta', { autoAlpha: 0, y: -35 }, 0); }
